# rtl8723bs wifi module loading
# only disable one, its either or

# disable staging driver, to use the alternative driver
blacklist r8723bs

# disable alternative driver, to test the staging driver
#blacklist 8723bs
options 8723bs rtw_power_mgnt=0 rtw_ips_mode=1 rtw_lps_level=0 rtw_smart_ps=2
